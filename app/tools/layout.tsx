import { ServiceSubpageFrame } from "@/components/ServiceSubpageFrame";

export default function ToolsLayout({ children }: { children: React.ReactNode }) {
  return <ServiceSubpageFrame>{children}</ServiceSubpageFrame>;
}
